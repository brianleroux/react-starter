var debug  = require("debug");
var path = require('path');
var manifest = require(path.join(__dirname, '../../package.json'));

module.exports = function(name){
  return debug(manifest.name + ':' + name);
};
